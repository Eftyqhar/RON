openai
SpeechRecognition
pyaudio
pywin32
pywhatkit
ddgs

# PDF generation. fpdf2 is the maintained fork and renders inline **bold**
# natively. The legacy `fpdf` 1.7.2 package also works -- tools.py detects it
# and emulates bold via write() -- but its PDF metadata path is not
# unicode-safe, so fpdf2 is preferred. Both install into the same `fpdf/`
# directory, so if legacy fpdf is already present, remove it first:
#     pip uninstall -y fpdf
fpdf2>=2.7.6

# HUD system monitor (ui_server.py). Optional: without it the interface runs
# normally and the CPU/RAM/GPU/DISK readouts show N/A.
psutil

# Persistent, DOM-first Chrome automation used by browser_agent.py.
selenium>=4.20.0
openpyxl>=3.1.0

# Accessibility-tree control of an already-open foreground browser tab.
uiautomation>=2.0.20

# High-fidelity Neural Text-To-Speech (JARVIS & Bengali neural voices)
edge-tts>=6.1.10
pygame>=2.5.0

# Desktop Floating Mini-HUD & System Tray Bar
pystray>=0.19.5
Pillow>=10.0.0
